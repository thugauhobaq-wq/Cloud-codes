# Развёртывание ботов

Инструкция для того, кто ставит бота на сервер: [`booking-bot`](booking-bot/),
[`shop-bot`](shop-bot/), [`leadmagnet-bot`](leadmagnet-bot/),
[`giveaway-bot`](giveaway-bot/), [`freelance-parser`](freelance-parser/),
[`botfactory`](botfactory/) и любой бот, созданный [`botkit`](botkit/). Все они устроены одинаково, поэтому шаги
общие — меняются только имя каталога и содержимое `.env`.

## Что нужно от сервера

| Требование | Значение |
| --- | --- |
| ОС | любая с Docker: Ubuntu, Debian, Alma |
| Память | от 1 ГБ (бот занимает ~150 МБ) |
| Диск | ~1 ГБ: образ 165 МБ плюс база, которая растёт медленно |
| Порты | никаких: боты работают на long polling и сами ходят к Telegram |
| Домен и сертификат | не нужны |

Нужен исходящий доступ к `api.telegram.org`. Входящих соединений бот не
принимает, поэтому пробрасывать порты и настраивать nginx не требуется.

## Аренда сервера

Если сервера ещё нет. Российские хостеры, оплата картой РФ, для самозанятого
есть чеки и договор: **Timeweb Cloud**, **Beget**, **RuVDS**, **Selectel**.
Разница между ними для наших задач невелика — берите, где удобнее платить.

Что заказывать:

| Параметр | Значение | Почему так |
| --- | --- | --- |
| Тип | **KVM**, не OpenVZ и не LXC | на контейнерных тарифах Docker часто не работает вовсе |
| ОС | Ubuntu 24.04 LTS | под неё написан скрипт подготовки |
| Память | 2 ГБ | на 1 ГБ боты работают, но сборка образа падает без подкачки |
| Диск | 20–30 ГБ | образ 165 МБ, база растёт медленно |
| Процессор | 1 ядро | боты почти всё время ждут ответа Telegram |
| Расположение | Россия или Европа | Telegram доступен и оттуда, и оттуда |

Это 300–500 ₽ в месяц. Тариф за 150 ₽ с 512 МБ брать не стоит: собирать образ
на нём нечем, а раскладывать сборку по частям — лишние полдня возни.

При заказе **добавьте свой ssh-ключ** в панели хостера (поле «SSH-ключи»).
Если ключа нет, сделайте на своём компьютере:

```bash
ssh-keygen -t ed25519            # Enter на все вопросы
cat ~/.ssh/id_ed25519.pub        # это и вставьте в панель
```

Пароль от root хостер пришлёт письмом — им можно войти, если ключ не добавили.

### Подготовка сервера одной командой

```bash
ssh root@адрес-сервера
curl -fsSL https://raw.githubusercontent.com/thugauhobaq-wq/Cloud-codes/main/server-setup.sh -o setup.sh
less setup.sh          # прочитайте: это чужой скрипт, который запускается от root
bash setup.sh
```

[`server-setup.sh`](server-setup.sh) заводит рабочего пользователя `bots`,
ставит Docker, закрывает вход по паролю, включает фаервол и автообновления
безопасности, добавляет подкачку на серверах меньше 2 ГБ и ограничивает логи
контейнеров. Повторный запуск безопасен — сделанное он не переделывает.

Две вещи, которые скрипт делает намеренно осторожно:

- **Пароль он закрывает, только если нашёл ssh-ключ.** Иначе вы остались бы
  снаружи собственного сервера. Нет ключа — предупредит и оставит как было.
- **Конфиг ssh применяется, только если сам sshd его принял.** Не принял —
  скрипт откатывает правку, а не оставляет сервер, в который не войти.

После него **не закрывайте окно**: откройте второе и проверьте, что заходите
как `ssh bots@адрес`. Если что-то пошло не так, первая сессия останется
рабочей, и всё можно починить.

## Установка

```bash
git clone https://github.com/thugauhobaq-wq/Cloud-codes.git
cd Cloud-codes/shop-bot                     # или booking-bot, leadmagnet-bot…
cp .env.example .env
nano .env                                   # BOT_TOKEN и OWNER_ID обязательны
docker compose up -d --build
docker compose logs -f
```

Токен даёт [@BotFather](https://t.me/BotFather), ваш user id покажет
[@userinfobot](https://t.me/userinfobot).

В логах при удачном старте:

```
13:23:01 INFO    shop: запускаюсь: Магазин
13:23:01 INFO    aiogram.dispatcher: Start polling
```

Каталог `data/` создаётся сам, права на него выставляются при старте — ничего
делать руками не нужно.

## Проверка, что всё работает

1. Напишите боту `/start` — он должен ответить меню.
2. Отправьте `/admin` — с вашего `OWNER_ID` придёт справка админа.
3. Пройдите короткий сценарий целиком: запись, заказ или выдача материала.
   Владельцу должно прийти уведомление.
4. `docker compose restart` — бот поднимается, данные на месте.

Полезно проверить и офлайн-командами, не запуская бота:

```bash
docker compose run --rm shop python -m shop catalog     # витрина
docker compose run --rm shop python -m shop orders      # заказы
```

`shop` здесь — имя сервиса из `docker-compose.yml`; у остальных это
`booking`, `leadmagnet`, `giveaway`, `fparser` и `botfactory`. Команды у
каждого бота свои и перечислены в его README.

`giveaway-bot` и `botfactory` собираются из корня репозитория, а не из своего
каталога: в образ нужен ещё и `botkit`, лежащий рядом. `docker compose` это
уже учитывает (`context: ..`), так что команды те же — но если собираете
руками, запускайте `docker build` из корня:

```bash
docker build -f giveaway-bot/Dockerfile -t giveaway-bot .
```

## freelance-parser: что отличается

Это не бот для клиентов, а личный инструмент: он опрашивает фриланс-биржи и
присылает подходящие заказы в ваш канал. Отличий от остальных три.

**Нужен канал, а не только токен.** В `.env` заполняются `BOT_TOKEN`,
`TARGET_CHAT_ID` (куда слать заказы) и `OWNER_ID` (кому разрешено менять
фильтры). Бота надо добавить в канал администратором с правом публикации,
иначе сообщения не уйдут.

**Нужен доступ к площадкам, а не только к Telegram.** Остальным ботам хватает
`api.telegram.org`; этому нужны ещё сами биржи. Часть из них с серверных
адресов отвечает 403 — это их защита от ботов, а не поломка. Поэтому до
запуска стоит выяснить, кто отвечает именно с вашего сервера:

```bash
cd Cloud-codes/freelance-parser
cp .env.example .env && nano .env
docker compose run --rm fparser python -m fparser dryrun --all
```

`dryrun` ничего не отправляет в канал. По каждой площадке он печатает либо
`найдено N`, либо ошибку с HTTP-кодом:

| Что видно | Что случилось | Что делать |
| --- | --- | --- |
| `найдено N`, N больше нуля | площадка работает | ничего |
| `ответил 403` или `429` | площадка не пускает серверные адреса | прописать `HTTP_PROXY_URL` в `.env` либо выключить её командой `/sources` |
| `ответил 404` | адрес ленты сменился | подставить новый в `FEED_URL_*` |
| `не задан …_TOKEN` | источник требует ключа | получить ключ или оставить источник выключенным |
| `найдено 0` без ошибки | адрес жив, но формат другой | `python -m fparser probe <площадка>`, затем чинить разбор по сохранённой фикстуре |

Состояние каждой площадки и список негодных — в
[`SOURCES.md`](freelance-parser/SOURCES.md).

**Первый запуск молчит — так и задумано.** Текущая лента помечается
просмотренной, иначе в канал прилетела бы сотня заказов, которые давно
разобрали без вас. Владельцу приходит одно сообщение об инициализации, дальше
идут только новые. Что сервис жив, показывает `/status`; заставить опросить
биржи немедленно — `/check`.

## Обновление

```bash
git pull
docker compose up -d --build
```

База лежит в `data/` вне контейнера и переживает пересборку. Схема
дополняется на месте при первом запуске новой версии: например, магазин,
который принимал заказы до появления оплаты, после обновления получает поля
оплаты, не теряя прошлых заказов.

## Автообновление из main

Чтобы не заходить на сервер после каждого изменения, на нём работает таймер:
раз в минуту он смотрит, не появился ли в `main` новый коммит, ждёт зелёный
CI на нём и пересобирает только те проекты, чьи файлы изменились. Отчёт
приходит в Telegram. Всё лежит в [`deploy/`](deploy/).

```bash
git clone https://github.com/thugauhobaq-wq/Cloud-codes.git /opt/cloud-codes
bash /opt/cloud-codes/deploy/install.sh
```

Установщик кладёт скрипт `cloud-codes-update`, включает таймер systemd,
открывает в фаерволе 80 и 443 и создаёт `/etc/cloud-codes/env` — там
список проектов и токен для отчётов (см. `deploy/env.example`). Секреты
только на сервере: `.env` проектов лежат в их каталогах и в git не попадают.

Как он себя ведёт:

- новый коммит с красным CI — не трогает, пишет «CI красный»;
- сборка упала — сообщает и ждёт следующего коммита, а не долбит каждую минуту;
- проект без `.env` пропускает и говорит об этом;
- файлы на сервере правили руками — останавливается и сообщает, чтобы не
  затереть правки;
- `cloud-codes-update --force` пересобирает всё сразу, не дожидаясь коммита.

Чтобы перевести под таймер уже работающего бота (например, `booking-bot`):
скопировать его `.env` и `data/` в `/opt/cloud-codes/booking-bot/`, добавить
`booking-bot` в `PROJECTS` в `/etc/cloud-codes/env`, остановить старый
контейнер (`docker compose down` в старом каталоге — один токен, один
экземпляр) и запустить `cloud-codes-update --force`.

Деплой из GitHub Actions по ssh здесь не используется намеренно: ветки
`claude/**` вливаются в `main` автомёржем без ревью, и секрет с ключом от
сервера прочитал бы любой будущий workflow. Таймер на сервере ничего
секретного в репозитории не требует.

## Бэкап

База — один файл, и снимать копию можно на ходу, не останавливая бота:

```bash
docker compose exec shop python -c "
import sqlite3
src = sqlite3.connect('/app/data/shop.db')
dst = sqlite3.connect('/app/data/backup.db')
with dst:
    src.backup(dst)
"
```

Так копия получается согласованной даже во время записи — этим `.backup`
отличается от `cp`, который может застать базу на середине транзакции.
Готовый файл лежит в `data/backup.db`. Копируйте его к себе хотя бы раз в
сутки: в базе заказы, записи и собранная база подписчиков — восстановить их
неоткуда.

Восстановление: остановить бота, положить файл на место рабочей базы,
запустить снова.

```bash
docker compose stop
cp backup.db data/shop.db
docker compose start
```

## Когда бот молчит

| Что видно | Что случилось | Что делать |
| --- | --- | --- |
| `Не заданы обязательные переменные окружения: BOT_TOKEN, OWNER_ID` | `.env` не заполнен | заполнить и `docker compose up -d` |
| `Unauthorized` в логах | неверный токен | взять токен заново у @BotFather |
| `Conflict: terminated by other getUpdates request` | тот же токен использует второй запущенный бот | оставить один экземпляр: `docker compose ps` на всех серверах |
| `unable to open database file` | каталог `data/` недоступен на запись | проверить, что том не смонтирован только для чтения |
| Контейнер в перезапуске | нет доступа к `api.telegram.org` | проверить сеть сервера; бот перезапускается сам и подхватится, когда связь появится |
| Первое сообщение осталось без ответа, в логах `TelegramNetworkError: Request timeout error` | сеть моргнула на отправке ответа | повторить сообщение: бот продолжает работать, отдельные таймауты он переживает сам. Если повторяется — сравнить `curl -4` и `curl -6` до `api.telegram.org`: сломанный IPv6 даёт ровно такую картину |
| Бот отвечает, админка молчит | `OWNER_ID` чужой | посмотреть свой id у @userinfobot |

Логи: `docker compose logs --tail 100`. Состояние: `docker compose ps`.

## Несколько ботов на одном сервере

Каждый бот — отдельный каталог со своим `.env`, `data/` и
`docker-compose.yml`. Они не мешают друг другу: портов не занимают, имена
контейнеров разные. Ограничение одно — **один запущенный экземпляр на токен**:
два бота с одним токеном отбирают друг у друга обновления, и оба работают
через раз.

## Что проверено

На чистом Docker собраны и запущены образы всех пяти ботов и обоих
вариантов проекта из `botkit` (в общем репозитории и автономного). Проверено:

- сборка образа с нуля и запуск офлайн-команд в контейнере;
- каталог `data/`, смонтированный с хоста: база создаётся и переживает
  пересборку образа;
- процесс внутри контейнера работает не под root (uid 1000);
- `docker compose up -d` поднимает бота, `docker stop` останавливает его
  мягко — за 0,1 секунды, а не по десятисекундному таймауту;
- незаполненный `.env` даёт понятное сообщение, а не трейсбек;
- недоступный Telegram завершает процесс с ненулевым кодом, и
  `restart: unless-stopped` поднимает бота заново.

Образ `botfactory` проверен без шага `apt-get install git`: в окружении сборки
закрыт доступ к репозиториям Debian. Остальная часть его образа — установка
пакета, права на томе, понижение привилегий — проверена так же, как у других.

`server-setup.sh` прогнан **на живом сервере Timeweb** — Ubuntu 24.04, KVM,
1900 МБ памяти, Москва. Отработали все шаги: Docker встал и демон запустился,
`ufw` закрыл всё, кроме ssh, создалась подкачка на 2 ГБ, вход по паролю
закрылся, и сервер после этого пускает по ключу. До того скрипт дважды подряд
прогонялся на чистой Ubuntu в контейнере — там проверялась идемпотентность и
обе развилки настройки ssh: с ключом `passwordauthentication` действительно
становится `no` (по выводу `sshd -T`, а не по факту записи в файл), без ключа
остаётся `yes`, и сервер не запирается.

`booking-bot` проверен на этом же сервере с настоящим токеном: образ
собрался, контейнер поднялся, `seed` залил демо-услуги, бот ответил на
`/start` меню и показал прайс. Первая попытка `/start` при этом отвалилась по
таймауту на отправке ответа, вторая прошла — отдельные сетевые сбои бот
переживает сам, перезапускать его не нужно.

Не проверено: остальные боты с живым токеном — они устроены так же, но
вживую пока запускался только этот.

`freelance-parser` стоит особняком. Его разбор, фильтры и отправка покрыты
тестами на сохранённых ответах площадок, но **ни одна биржа вживую не
опрашивалась**: из окружения разработки они закрыты сетевой политикой — закрыт
даже `example.com`, так что дело не в защите бирж. Кто реально отвечает с
сервера, покажет первый `dryrun --all`, и результат записывается в
[`SOURCES.md`](freelance-parser/SOURCES.md).

Автообновление (`deploy/`) проверено без сервера: 26 сценариев на локальном
«origin» с подменёнными `docker` и Telegram — первый запуск, тик без
изменений, коммит в один проект, красный и незавершённый CI, упавшая сборка,
ручные правки на сервере, `--force`. Compose-файлы `handwriting-ocr` с
HTTPS-надстройкой складываются, порт 8080 наружу не торчит. На живом VPS
установщик и таймер пока не запускались.
