name: booking-bot CI

on:
  push:
    paths:
      - 'bots/booking-bot/**'
      - '.github/workflows/booking-bot-ci.yml'
  pull_request:
    paths:
      - 'bots/booking-bot/**'
      - '.github/workflows/booking-bot-ci.yml'

defaults:
  run:
    working-directory: bots/booking-bot

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: bots/booking-bot/pyproject.toml

      - name: Установить зависимости
        run: pip install -e ".[dev]"

      - name: Линтер
        run: ruff check src tests

      - name: Тесты
        # Ни сети, ни Telegram: всё работает на временной базе SQLite
        # и подставных отправителях.
        run: pytest -q

      - name: Прогон офлайн-команд
        run: |
          python -m booking seed
          python -m booking slots
        env:
          DB_PATH: /tmp/ci-booking.db
          BOT_USERNAME: ci_bot
